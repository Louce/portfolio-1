# Required for dynamic site URL in metadata and Open Graph tags
# Used in src/app/layout.tsx
# For local development, defaults to http://localhost:9002 or PORT
NEXT_PUBLIC_SITE_URL=http://localhost:9002

# Optional: Port for development server (defaults to 3000 if not set in package.json)
# Used in package.json script and src/app/layout.tsx
PORT=9002

