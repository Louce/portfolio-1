import * as feedbackService from './feedbackService';

/**
 * @fileoverview Barrel file for exporting all service modules.
 * This allows for cleaner imports in other parts of the application.
 * e.g., `import { feedbackService } from '@/services';`
 */
export { feedbackService };
